import React, { FC } from 'react';
import { VaguReactPlayer } from '@lib/index';

const App: FC = () => {
  return (
    <div className="App">
      <VaguReactPlayer />
    </div>
  );
};

export default App;
